import colors from './colors'
import metrics from './metrics'
import shadows from './shadows'
import typography from './typography'

export default {
  colors,
  metrics,
  shadows,
  typography,
  name: 'BAM main theme',
  space: [0, 2, 4, 8, 16, 24, 32],
  radii: [2, 4, 8, 16, 24, 32],
  fontSizes: [12, 16, 24, 32],
  lineHeights: [15, 19, 27, 35],
}
